# 과제2 리포트
**1. 상품코드를 입력하면 즉시 재고 수량을 확인해야 하는 재고 관리 데이터**
1. dict
2. 상품코드를 key, 재고 수량을 value로 저장하면 상품코드를 이용해 재고 수량을 빠르게 확인할 수 있다.  
list는 원하는 상품을 찾기 위해 순차적으로 탐색해야 하므로 적합하지 않다.
tuple은 데이터를 순서대로 묶는 데 적합하지만 상품코드와 재고 수량을 연결하여 검색하기에는 불편하다.
set은 중복 제거와 포함 여부 확인에는 적합하지만 상품코드에 대응하는 재고 수량을 저장하기 어렵다.
3. ```python
    재고= {"a01":17, "a02":3}
    a= "a01"
    print(재고[a])
    ```
**2. 이벤트 응모 고객 명단에서 중복 응모를 제거하고, 기존 회원 명단과의 교집합을 구해야 하는 상황**
1. set
2. list와 tuple은 중복을 허용하며 집합 연산이 불가하다. dict는 key의 중복이 허용하진 않지만, 집합 연산을 지원하지 않아 set보다 불편하다.
3. ```python
    고객={"정시원","정찬영","전서진"}
    회원={"정시원"}
    교집합=고객&회원
    print(교집합)
    ```
**3. 월별 매출액을 1월부터 12월까지 순서대로 저장하고 순회하는 데이터**
1. list
2. 순서대로 저장하고 순회하는데 있어서 list가 적절하다. tuple도 이와 같지만 매출액 변경 가능성이 있으므로 부적절하다. set은 순서가 없으며 dict은 가능하긴 하나 list를 활용하는게 더 간단하다.
3. ```python
    매출=[100,344,450,590,480,3330,40,450,560,230,10,30]
    for i in 매출:
        print(i)
    ```
**4. 한 번 발급되면 절대 변경되어서는 안 되는 (위도, 경도) 매장 좌표**
1. tuple
2. tuple은 생성 후 원소를 변경할 수 없기 때문에 좌표처럼 변경되어서는 안 되는 데이터를 저장하기에 적절하다. tuple 외의 set, dict,list는 전부 데이터가 수정이 가능하다.  또, 위도와 경도는 묶음으로서 좌표를 나타내므로 tuple이 적절하다.
3. ```python
    좌표=(23.45, 456.333)
    위도=좌표[0]
    경도=좌표[1]
    위도=45.66666 #error
    print(좌표,위도,경도)
    ```
**5. 1,000만 줄짜리 웹 서버 접속 로그 파일에서 특정 조건의 줄 수를 세는 작업 (자료구조 + 처리 방식 관점에서 서술)**
1. iterator/Generator
2. 모든 로그를 메모리에 저장하는 별도의 자료구조를 사용하지 않고 파일을 iterator 방식으로 한 줄씩 처리하며 Generator를 이용해 조건에 맞는 로그만 순차적으로 전달하는 것이 적절하다. 별도의 쌍으로 묶어야할 데이터가 없으므로 list와 tuple대신 굳이 set와 dict을 사용할 필요는 없다. 그러나, 모든 접속 로그를 list나 tuple에 올리는 것은 데이터를 과하게 차지한다. 따라서, iterator을 이용해 파일을 한줄씩 순차적으로 읽으며 조건에 맞는 줄만 세면 된다.
3. ```python
    def count_errors(path):
        with open(path, encoding="utf-8") as f:
            for line in f:
                if "error" in line: yield 1
    error_count= sum(count_errors("server.log"))
    print("error 로그:",error_count)
    ```